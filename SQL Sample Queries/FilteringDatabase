/*Selecting movies from database */ 
SELECT * FROM movies;

/*Selecting only movies in the database that were released 2000 or later */
SELECT * FROM movies
WHERE release_year > 2000
ORDER BY release_year

